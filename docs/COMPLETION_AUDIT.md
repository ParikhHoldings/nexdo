# Completion Audit

Date: 2026-05-17

## Objective restated
Make Nexdo a credible, launchable early-access product by Monday, 2026-05-18, with:
- an actual working product front-end instead of a non-functional shell
- immediate and long-term plans for human users and AI-agent users
- core launch features filled in for the MVP path
- verified build/test rails
- clear evidence for what is done, unverified, blocked, or deferred

## Prompt-to-artifact checklist

| Requirement | Evidence | Status |
| --- | --- | --- |
| Read and understand existing Markdown/text files | `README.md`, `AGENTS.md`, docs updated after reading all repo Markdown/text files including `public/robots.txt` and `.github/pull_request_template.md` | Done |
| Update `AGENTS.md` for Codex/LLM project understanding | `AGENTS.md` now includes product truth, architecture, commands, env contract, verification state, and doc rules | Done |
| Provide immediate plan | `docs/LAUNCH_PLAN.md` Monday 2026-05-18 section | Done |
| Provide long-term plan | `docs/LAUNCH_PLAN.md` human-user, AI-agent, and long-term direction sections | Done |
| Document minimum human-user and AI-agent MVP path | `docs/MVP.md` defines the capture -> structure -> prioritize -> brief -> bounded execution path, AI-agent task-layer requirements, acceptance gates, blockers, and post-MVP hardening queue | Done |
| Make front-end usable for humans today | Logged-out `/today` demo flow supports task capture with a labeled submit button and Enter-key submission, structured parsing, `/quick` capture guardrails, briefing, prioritization, task detail, visible non-default statuses, cancelled-task review/restore, and bounded agent output with review notes/history | Locally verified |
| Preserve due time through task capture | Parsed task output includes `due_time`; deterministic fallback parsing extracts common local times and keeps common schedule, priority, relative-date connector, and estimate phrases out of the title; `TaskInput`, authenticated task creation, and MCP `create_task` persist parsed due times; OpenAI smoke expectations cover parse due time | Locally verified |
| Reject invalid task dates and times | Shared local date/time normalizers now back human task creation/patch validation, AI task sanitization/output validation, deterministic fallback parsing, import parsing, and MCP `update_task` handling so impossible dates such as 2026-02-30 and out-of-range times such as 29:00 fail closed; focused helper, task-intelligence, importer, and MCP handler tests cover these rails | Locally verified |
| Use due time in planning context | OpenAI prioritization/briefing summaries include due time and deterministic prioritization orders same-day timed work by due time | Locally verified |
| Keep briefings fresh for structured task changes | The daily briefing signature includes due time, context, action type, estimate, energy, people, and tags; local briefing treats past due times today as overdue with UI copy for past-due-today items | Locally verified |
| Keep authenticated task capture honest on save failure | Authenticated task creation no longer falls through to a local demo task when `/api/tasks` fails or Supabase returns no active user for a stale authenticated store; the input is restored and API-provided profile/quota/save failure messages are shown when available | Done |
| Keep briefing aligned with task changes | Logged-out daily briefing refreshes from local task state after demo task capture/import; authenticated briefing now refetches when task state or user name changes and shows a local-briefing fallback notice when the provider path fails or rate-limits; Playwright verifies the demo active-task count updates after adding a task | Locally verified for demo; implemented locally for authenticated |
| Keep demo task changes across reloads | Logged-out task add/edit/delete/complete flows now persist demo task state to localStorage; Playwright verifies a new demo task survives reload and new IDs remain unique afterward | Locally verified |
| Verify demo workspace navigation and task lifecycle | Playwright now covers mobile sidebar open/close and post-navigation collapse, `/all` search/filtering, `/upcoming` date grouping, completing a task into `/done`, persistence after reload, and clearing completed demo tasks; authenticated Done-page bulk delete failures restore failed tasks locally and surface API-provided delete messages before generic fallback text | Locally verified for demo; implemented locally for authenticated failure rollback |
| Add task notes for human and agent context | Task detail now exposes a notes panel for decisions, links, and handoff context; demo notes persist to localStorage through `lib/task-notes.ts`, authenticated notes use owned-task `GET/POST /api/tasks/[id]/notes`, server-managed note metadata is written through the service-role note route after ownership checks, direct note content inserts are bounded by the database to non-empty content up to 2,000 characters, MCP/ChatGPT Actions `add_task_note` appends bounded notes to owned tasks, `get_task` returns recent notes, Playwright verifies note save/reload plus note route and MCP note guardrails, and `npm run smoke:app` can verify authenticated note routes against a real app session | Locally verified for demo and MCP handler; real Supabase/app/MCP verification missing |
| Keep demo profile settings functional | Logged-out/no-auth profile changes save to localStorage instead of failing against the authenticated profile API; Playwright verifies demo name/timezone persist across reloads | Locally verified |
| Make appearance settings functional | Settings now includes a real Appearance tab backed by the UI store, `localStorage`, a theme controller, and light-mode CSS mappings; Playwright verifies light/dark selection applies to the document and survives reload | Locally verified |
| Make notification settings functional | Settings now includes a real Notifications tab backed by local browser notification permission, `localStorage`, and a due-task notification controller; Playwright stubs the browser Notification API and verifies permission request, reminder delivery, persistence, and once-per-task-per-day replay prevention | Locally verified |
| Keep demo users out of authenticated UI state | Demo profiles no longer mark `useUserStore` authenticated, preventing signed-in-only controls such as sign-out from appearing in no-auth demo mode; Playwright covers the absence of sign-out in demo settings | Locally verified |
| Keep authenticated app boot state coherent | Authenticated app boot now sets both user and task auth state when Supabase has a user; missing/delayed profile rows use a safe client-only fallback profile instead of leaving the app half-authenticated, and profile/task load failures surface visible errors | Done |
| Make import front-end usable in demo mode | Logged-out file imports now parse CSV, JSON, and ICS client-side instead of hitting authenticated import APIs; Playwright verifies a CSV import appears in the task list without configured auth | Locally verified |
| Preview file imports before mutation | File import cards now parse selected CSV/JSON/ICS files client-side, show task count and sample titles, warn about demo caps or Free-plan remaining slots, and require a confirm click before tasks are added; Playwright verifies preview and confirm for demo CSV import, and focused helper coverage verifies demo caps plus Free-plan near-limit warning copy | Locally verified; real Supabase profile near-limit smoke still required |
| Keep authenticated imports visible after success | Authenticated import responses now add returned server-side tasks into the local task store so successful imports appear in the workspace without a reload; focused Playwright source coverage guards against the old demo-only hydration condition | Implemented locally; real provider/import verification missing |
| Keep connected imports honest and usable | Todoist uses a personal API token; Google Tasks and Microsoft To Do now expose manual access-token imports wired to existing provider routes while clearly marking full OAuth connection as post-launch; routes accept the frontend `token` payload alias; Playwright verifies the rendered manual-token path and absence of inert coming-soon cards | Locally verified for UI; real provider verification missing |
| Keep due-today behavior on local calendar dates | Shared local date-key helpers now back demo seed due dates, Today/sidebar filtering, Today focus/sidebar/briefing count alignment for undated active tasks and cancelled-only work, demo upcoming filtering, All Tasks due-date sorting, browser reminder delivery, MCP due-today filtering, OpenAI briefing/smoke date context, date-only import parsing, and relative date labels so behavior does not drift after UTC midnight | Locally verified |
| Keep bounded agent outputs reviewable | Agent execution now stores a versioned output envelope with current result, run history, and verification status/note; task detail lets demo users mark output verified/needs revision and rerun to preserve history; authenticated review notes save through a dedicated owned-task route | Locally verified for demo; provider verification missing |
| Cover representative import parsing | `tests/e2e/importers.spec.ts` verifies Todoist, CSV with auto mapping, ICS VTODO metadata, Trello JSON, Things-style JSON, and invalid JSON parser behavior | Done |
| Keep demo entry reachable when env is placeholder/missing | Supabase client/server/middleware/login checks now reject placeholder anon/service keys as unconfigured; Playwright verifies the login page exposes demo mode without auth env | Locally verified |
| Keep demo useful without provider secrets | `lib/task-intelligence.ts`, `lib/openai.ts`, `components/task-input.tsx`, `components/daily-briefing.tsx`, `components/task-detail.tsx`, `app/(app)/today/page.tsx` | Done |
| Cover deterministic fallback behavior | `tests/e2e/task-intelligence.spec.ts` verifies local parsing metadata, prioritization order, briefing sections, and bounded research/draft/prep execution outputs | Done |
| Harden AI briefing/prioritization inputs | `lib/ai-task-input.ts` sanitizes task arrays and user names before `/api/tasks/prioritize` and `/api/briefing` consume rate limits or call OpenAI/fallback execution; authenticated briefing/prioritization UI surfaces provider/rate-limit fallback notices instead of silently switching to local heuristics | Done |
| Cover local validation and limit helper contracts | `tests/e2e/helper-rails.spec.ts` verifies AI task input validation/defaulting, API-key scope mapping, quota response payloads/status mapping, and rate-limit response headers | Done |
| Harden AI provider output shapes | `lib/ai-response-validation.ts` validates and bounds OpenAI JSON responses for parsing, prioritization, briefing, and research/draft/prep output before runtime helpers return or persist provider content; malformed provider output falls back to local heuristics | Done |
| Keep agent prompt boundaries honest | `lib/prompts.ts` frames agent work as bounded, reviewable research/draft/prep output and tells the model to hand external side effects or judgment back to the user; Playwright source coverage guards against broader automation language | Done |
| Harden core task mutation routes | `lib/task-validation.ts` validates and normalizes task-create payloads before quota/insert; `POST /api/tasks` pre-checks quota, inserts the owned task through the service-role path so validated non-agent source values stay server-mediated, then records usage with best-effort cleanup if accounting fails; `PATCH /api/tasks/[id]` uses the same shared rail to allowlist user-editable fields, reject protected/server-managed fields such as `user_id`, `completed_at`, `source_agent_id`, and `agent_output`, then writes server-managed completion timestamps through the service-role path and returns 404 when no owned task is updated; `DELETE /api/tasks/[id]` returns 404 when no owned task is deleted; e2e covers unauthenticated/config guardrails and focused helper tests cover create/patch validation contracts | Done |
| Keep task mutation failures visible | Authenticated task edit/delete failures now roll back optimistic local state and surface task-store errors through the app toast layer, including API-provided failure messages when available, instead of console-only failures | Done |
| Make task detail editing functional | The task detail panel now has edit mode for title, context, due date, due time, priority, action type, estimate, energy level, people, and tags; the status selector includes `cancelled`; task cards surface non-default statuses including waiting and cancelled; Playwright verifies demo task edits are visible before agent execution | Locally verified |
| Harden profile mutation route | `PATCH /api/profile` now validates name length, allowlisted timezone values, and allowlisted work types before updating profiles, signup trims and bounds `full_name` before Supabase profile creation, and profile updates return 404 when no profile row is updated | Done |
| Harden profile/database access boundary | `supabase/migrations/005_hashed_api_keys.sql` hashes stored API keys; `supabase/migrations/006_profile_column_grants.sql` limits direct authenticated profile reads/updates to app-needed fields; `supabase/migrations/011_profile_content_constraints.sql` enforces profile name and timezone bounds for direct browser preference updates; `supabase/migrations/012_profile_insert_grants.sql` revokes direct browser profile inserts so profile rows remain auth-trigger/service-owned; `supabase/migrations/013_stripe_event_grants.sql` keeps Stripe webhook idempotency records service-owned; `supabase/migrations/014_usage_rate_limit_grants.sql` keeps usage-event mutations and rate-limit buckets service-owned; `supabase/migrations/015_daily_briefing_grants.sql` keeps cached daily briefing writes service-owned; `supabase/migrations/007_task_column_grants.sql`, `009_task_source_grants.sql`, and `016_task_relationship_grants.sql` limit direct authenticated task insert/update columns so the broad task source flag, agent output, source-agent metadata, ingestion intent, completion timestamps, and task relationship metadata remain server-managed; `supabase/migrations/010_task_content_constraints.sql` enforces the core task content bounds at the database layer for direct browser writes; `supabase/migrations/008_task_note_column_grants.sql` limits direct authenticated task-note inserts so note type and creation time remain server-managed and adds the database note-content length constraint; sensitive billing, Stripe webhook idempotency, raw/hash API-key, quota/rate-limit mutation, entitlement state, task source, imported completion/external refs, agent task state, briefing cache writes, task relationship metadata, and note metadata use server/service-role paths; Playwright regression coverage checks the profile/task/task-relationship/task-note/Stripe-event/usage/rate-limit/daily-briefing grants and service-role task/create/import/note/webhook/quota write paths | Implemented locally; real Supabase verification missing |
| Harden authenticated agent execution | `/api/agent/execute` now requires a `taskId`, loads the owned task from Supabase, rejects non-executable task types through the shared `lib/task-actions.ts` executable contract, preflights the service-role output persistence path before rate-limit/quota/provider work, maps quota pre-check and post-run accounting failures through the shared quota status helper, records quota usage after a successful run, and only then returns saved `agent_output`; task detail surfaces API-provided execution/review failure messages; generic task PATCH rejects client-supplied `agent_output`; Playwright verifies reminder tasks do not expose AI-agent controls | Done |
| Enforce task quotas on imports | CSV, JSON, ICS, Todoist, Google Tasks, and Microsoft To Do imports now pre-check requested task quota, save imported tasks through the service-role path after user auth, then consume quota only for successfully inserted rows; inserted rows are returned with IDs so quota-accounting failures can clean them up; Google Tasks and Microsoft To Do fail closed when a nested provider task-list fetch fails instead of silently importing partial data; import UI surfaces API-provided quota/profile failure messages before generic errors; migration `004_quota_noop_audit_cleanup.sql` prevents quota read probes from writing zero-quantity audit noise | Done |
| Harden file-import auth ordering | CSV, JSON, and ICS import routes now verify Supabase/auth before reading uploaded files or request bodies; e2e covers unauthenticated/config guardrails for all three | Done |
| Restore strict build rails | `next.config.mjs` no longer ignores TypeScript/ESLint; scripts include `typecheck`; `npm run build` passes | Done |
| Add browser smoke coverage | `playwright.config.ts`, `tests/e2e/demo-smoke.spec.ts` | Done |
| Add AI-agent and billing guard smoke coverage | `tests/e2e/agent-surfaces.spec.ts` covers OpenAPI schema, auth failures, action CORS headers, unsupported Stripe checkout plan rejection, quota failure mapping, Connect AI API-setting deep links, and the task column-grant/service-role write boundary for agent/import paths | Done |
| Cover DB-backed MCP tool handlers and API-key validation locally | `tests/e2e/mcp-tool-handlers.spec.ts` exercises owned list/search/get/briefing behavior, get-task note readback, create-task idempotent replay, quota pre-check, parse/insert/post-insert quota ordering, cleanup on quota-accounting failure, structured update fields, note appends, complete mutations, validation failures, missing/unowned task errors, `agent_action_events` audit logging, hashed API-key lookup, legacy raw-key migration, and paid-plan API access gating through an in-memory Supabase double | Done |
| Keep password recovery deployment-local | Password reset emails now prefer the current browser origin before falling back to `NEXT_PUBLIC_APP_URL`, preventing preview/local recovery links from pointing at the wrong deployment; reset emails are trimmed before provider calls; Playwright verifies source ordering and the no-auth rendered reset form | Locally verified; provider email delivery not smoke-tested |
| Keep ChatGPT Actions spec bound to the serving origin | `/api/mcp/openapi` now emits the request origin when `NEXT_PUBLIC_APP_URL` is not configured, so preview/local deployments do not advertise `nexdo.ai`; Playwright verifies the local server URL | Done |
| Add CI verification | `.github/workflows/verify.yml` runs on pull requests and pushes to `main`/`staging`, with install, lint, typecheck, build, dependency audit, and Playwright smoke tests; the workflow uses `actions/checkout@v5` and `actions/setup-node@v5` to avoid deprecated Node 20 action runtimes while keeping app verification on Node 22; Playwright source coverage guards the documented Web rails so dependency audit and branch triggers do not drift out of CI | Done |
| Tighten public copy and demo entry | `app/(marketing)/page.tsx`, auth pages, metadata, manifest, legal pages, docs guardrails; landing page primary CTA now points to the verified `/today` demo path, signup exposes a direct demo path, metadata/manifest lead with early-access task workspace language, legal copy avoids broad automation/security claims, and public copy avoids internal "front-door launch claim" language, with Playwright coverage | Draft tightened; Quill/founder approval still required before public use |
| Keep public crawl surface internally consistent | `app/sitemap.ts` now serves the public sitemap advertised by `public/robots.txt`, limited to landing, privacy, and terms pages; Playwright verifies `/sitemap.xml` exists and avoids app/API routes | Done |
| Remove dependency audit blocker | Next.js 16, ESLint 9 flat config, PostCSS override; `npm audit --audit-level=moderate` reports 0 vulnerabilities | Done |
| Verify deploy target and production env | `npm run verify:env` was rerun on 2026-05-17 and failed because `.env.local` is absent; no production env or deploy target credentials/config were exercised in this pass | Missing |
| Keep env verification aligned with runtime placeholder rules | `scripts/verify-env.mjs` and shared runtime helper `lib/env.ts` reject common placeholder fragments across URL and secret values; supplied env files override matching shell exports during verification so copied launch-smoke env files are checked directly; `.env.local.example` documents local env requirements and `.env.production.local.example` provides a concrete preview/production smoke env template with smoke-only MCP/API-key and Vercel bypass variables separated from deployed runtime env | Done |
| Document local and production setup steps | `docs/DEPLOYMENT.md` now separates no-provider local setup, env preflight, production setup sequence, provider smoke commands, rollback notes, and approval boundaries | Done |
| Verify preview deploy rail | PR #3 passed GitHub Actions Web rails on the latest inspected code head before this docs refresh, including the login-email normalization change; Vercel preview deployment previously passed on an inspected 2026-05-17 code head, but the latest inspected code head hit the known Vercel account build-rate limit, so current heads still need fresh preview-deploy evidence; direct route smoke against the current preview URL failed at `/` because Vercel Deployment Protection is active until `VERCEL_AUTOMATION_BYPASS_SECRET` or an unprotected preview URL is available | GitHub Web rails verified; fresh Vercel preview evidence missing on latest inspected head; remote route smoke blocked by Vercel auth |
| Verify Supabase migrations/auth/RLS/task CRUD against real project | Migrations and code exist, but real project smoke test was not run | Missing |
| Provide a repeatable Supabase smoke command | `npm run smoke:supabase` checks schema columns; `npm run smoke:supabase -- --write` creates/deletes a smoke auth user, verifies profile trigger, allowed profile reads/edits, denied direct profile inserts, denied sensitive profile reads/edits, denied profile content-bound bypasses, task CRUD through RLS, task-note content insert through RLS, direct browser denial for task source spoofing, task relationship writes, task content-bound bypasses, and other server-managed columns, direct browser denial for task-note metadata columns, direct browser denial for over-limit task-note content, agent external-ref uniqueness, public audit-event privacy, browser audit-event insert denial, service-role Stripe event insert plus public/browser read/write denial, usage-event mutation denial, rate-limit bucket privacy, daily briefing cache write denial, quota increments, quota no-op behavior, and rate-limit allow/block behavior | Done |
| Provide a repeatable authenticated app API smoke command | `npm run smoke:app` creates and deletes a disposable Supabase user, signs in through the app cookie flow, verifies `GET/POST /api/tasks`, `PATCH/DELETE /api/tasks/[id]`, task-note validation, `POST /api/tasks/[id]/notes`, `GET /api/tasks/[id]/notes`, seeded reviewable agent output, `PATCH /api/tasks/[id]/agent-review` validation/save behavior, and uses `VERCEL_AUTOMATION_BYPASS_SECRET` for protected Vercel previews when present | Done |
| Verify OpenAI provider-backed parse/prioritize/briefing/execution | Fallbacks and UI path work; real provider calls not exercised | Missing |
| Provide a repeatable OpenAI smoke command | `npm run smoke:openai` rejects missing/placeholder keys and verifies OpenAI JSON-mode calls for parse, prioritization, briefing, research, draft, and prep execution shapes; `npm run smoke:openai -- --app` creates a disposable Supabase user, verifies authenticated app parse, prioritize, briefing, and research/draft/prep execution routes when target app/Supabase env is loaded, and uses Vercel protection bypass headers when configured; app helpers use the same optional `OPENAI_MODEL` default | Done |
| Verify Stripe checkout/portal/webhook/quota updates | Code exists; Stripe test-mode flow not exercised | Missing |
| Provide a repeatable Stripe smoke command | `npm run smoke:stripe` verifies account and recurring price configuration; `npm run smoke:stripe -- --write` creates disposable test-mode customer, checkout session, and billing portal session; `npm run smoke:stripe -- --write --webhook` creates disposable Stripe/Supabase test data, verifies authenticated app checkout and billing portal routes, posts signed subscription and failed-payment webhook events to the app, uses Vercel protection bypass headers when configured, verifies unknown-price fail-closed behavior, verifies free/pro/power profile tier transitions, verifies payment-failure downgrade to Free, verifies quota plan-state boundaries for the resulting tiers, signs in the disposable user and exercises authenticated `POST /api/tasks` quota behavior under those tiers, verifies duplicate-event idempotency, and cleans up | Done |
| Harden Stripe plan selection and entitlement mapping | `/api/stripe/checkout` now accepts only `pro` or `power` plan keys, derives price IDs from server env, and fails if the Stripe customer ID cannot be persisted; settings billing actions surface API-provided checkout/portal failure messages before generic errors; webhooks normalize Stripe customer/subscription IDs from string or expanded-object event shapes, skip unknown Stripe prices instead of defaulting to paid access, and fail for retry if entitlement profile writes error, match no profile row, or event idempotency recording fails; `tests/e2e/stripe-entitlements.spec.ts` verifies missing, placeholder, unknown, and configured price IDs plus checked profile-tier writes; e2e covers unsupported checkout plans | Done |
| Keep pricing UI aligned with billing reality | `components/pricing-table.tsx` now shows monthly pricing only because annual Stripe prices are not configured; the landing-page pricing CTAs route to demo, signup, or sales email instead of starting unverified public checkout; public plan bullets avoid unverified SSO, admin controls, team collaboration, and custom integration promises; settings billing still uses authenticated checkout handlers | Done |
| Verify authenticated MCP/API-key flow against real task data | OpenAPI/auth/scope guardrails pass; real API-key tool execution not exercised | Missing |
| Provide a repeatable MCP/API-key smoke command | `npm run smoke:mcp` supports OpenAPI availability, authenticated MCP initialized-notification handshake, authenticated MCP SSE endpoint discovery, authenticated JSON-RPC list/search/briefing/get/structured-update/add-note/complete checks, ChatGPT Actions list/search/add-note response-shape checks, optional `NEXDO_READONLY_API_KEY` scope-denial checks, explicit `--write` task/idempotency and completed-task trace checks, `--provision` disposable full/read-only Power-plan API keys when Supabase service-role env is loaded, `--write --audit` verification of real `agent_action_events` rows, and Vercel protection bypass headers when configured | Done |
| Keep MCP bootstrap compatible with real clients | `/api/mcp` now accepts `notifications/initialized` without a JSON-RPC `id` and returns `204`, while still requiring an `id` for request/response methods; e2e source checks and `npm run smoke:mcp` cover the initialized notification path | Done |
| Provide a single technical launch smoke command | `npm run smoke:launch` loads a supplied env file into child processes, requires a remote HTTPS app URL for provider smokes unless `--allow-local-url` is intentionally used, runs lint, typecheck, build, Playwright, dependency audit, env preflight, rendered route smoke, Supabase write smoke, authenticated app smoke, OpenAI app-route smoke, Stripe webhook smoke, and MCP provisioned write/audit smoke; `--technical-only` keeps approval gates from being implied, while final launch mode requires explicit `--copy-approved` and `--production-deploy-verified` flags | Done |
| Make agent-created work distinguishable | MCP create/update/add-note/complete schemas and handlers expose `source_agent_id`, `external_ref`, `ingestion_intent`, and `agent_metadata`; writes that include `external_ref` require `source_agent_id`; MCP execution creates an `agent_action_events` row before running the tool handler and fails before task mutation if audit logging is unavailable; MCP create/update/complete writes persist trace metadata on task rows so task cards and task detail can show externally created, updated, or completed work, while note writes are auditable in `agent_action_events` | Done |
| Harden MCP tool input, search, and quota rails | MCP handlers validate bounded IDs, refs, metadata, query, structured task updates, bounded task-note appends, task completion trace metadata, and status/priority/planning values; MCP tool schemas and OpenAPI advertise the accepted task status contract including `cancelled` plus nullable due-date/context clears on `update_task`; humans can inspect cancelled agent work through the All Tasks cancelled filter and restore it from task detail; `search_tasks` and All Tasks search use shared metadata matching across title, context, description, people, and tags; `create_task` replays idempotent refs before quota, pre-checks task quota before parsing, creates the task, then records usage with cleanup if accounting fails; `get_task`, `update_task`, `add_task_note`, and `complete_task` return stable not-found errors for missing or unowned task IDs; OpenAPI exposes key length, note-length, and structured-update limits | Done |
| Keep ChatGPT Actions responses aligned with the advertised contract | `/api/mcp/actions/[tool]` formats tool output through `lib/mcp-action-results.ts`; Playwright verifies list/search `{ tasks }`, task `{ task }`, briefing object, validation error, and service-unavailable shapes; OpenAPI advertises `ErrorResponse` for tool validation failures | Done |
| Align API access with paid plan truth | API-key generation now requires a Power/team profile before rate-limit consumption; MCP API-key validation rejects keys for non-API tiers; settings UI disables free-plan key generation; e2e verifies the demo free-plan API tab guard | Done |
| Keep settings failures visible | Profile save failures now surface validation/API messages, successful authenticated saves refresh local profile state, and API-key generation failures render visible errors instead of console-only failures | Done |
| Keep launch controls backed by real behavior | Appearance and Notifications settings now change real local app state and are covered by Playwright; unbacked pricing controls such as annual checkout were removed until provider configuration exists | Done |
| Keep login input tolerant | Login now trims submitted email addresses before Supabase sign-in and updates the visible field, preventing pasted whitespace from blocking valid users; Playwright source coverage verifies the normalized email is used | Locally verified; real auth smoke still required |
| Keep auth redirects same-origin and visible | `lib/auth-redirect.ts` now bounds login redirects and callback `next` paths to same-origin app paths, falls back to `/today` for external/missing values, maps callback failures to a visible login-page error, and middleware preserves path plus query when sending protected-route visitors to login; helper and rendered Playwright coverage verify the contract | Locally verified |
| Keep settings deep links reliable | Settings tab clicks now update the URL query and `?tab=` query changes derive the active tab, so billing/API/appearance links keep landing on the intended tab after users click around; Playwright verifies billing, API, and upgrade-tab navigation | Locally verified |
| Align Connect AI setup with paid API gate | `/settings/mcp` now shows the Power-plan API access requirement before users try to generate/connect keys, treats old key hints as disabled when API access is inactive, disables the connection tester/activity/tools until API access and a key exist, tests full keys by initializing MCP and verifying `tools/list`, normalizes test-connection and agent-activity errors across plain, message, and JSON-RPC payloads, shows mutation intent plus safe argument-key summaries in agent activity, clarifies that setup clients need the full one-time key rather than the stored key hint, deep-links no-key users to `/settings?tab=api`, and Settings > API links paid users with keys into `/settings/mcp`; Playwright verifies the billing deep link, disabled free-plan tester, tool-discovery tester source rail, API-tab no-key guidance route, and API-to-Connect-AI handoff source rail | Done |
| Make upgrade/settings links land on the right tab | `/settings?tab=billing` now opens the billing tab, matching quota/API upgrade URLs; e2e verifies the query-param tab behavior | Done |
| Add idempotency and safer conflict handling for agent writes | `create_task` replays by `source_agent_id` + `external_ref`, migration adds `tasks_user_agent_external_ref_unique_idx`, and `npm run smoke:mcp -- --write` checks replay behavior | Implemented locally; provider verification missing |
| Add scoped API keys and agent audit trails | `supabase/migrations/003_agent_governance.sql`, settings key-scope UI, API-key rotation rate limiting, checked hashed-key persistence before one-time reveal, hashed key storage in `supabase/migrations/005_hashed_api_keys.sql`, prerequisite- and scope-aware MCP setup UI, MCP scope filtering/enforcement, audit-row preflight/finalization around MCP tool execution, `/api/mcp/events` with explicit user ownership filtering, and the MCP settings activity list exist; real Supabase migration/audit-write smoke still required | Implemented locally; provider verification missing |

## Commands verified locally
- `npm ci`
- `npm run lint`
- `npm run typecheck`
- `npm run build`
- `npm run test:e2e` passed 101 tests including public landing/signup demo CTA smoke, logged-out task capture/edit/reload persistence/agent output, labeled task-capture submit behavior, task-detail notes save/reload behavior, task-note route auth/validation guardrails, MCP `add_task_note` append/readback and audit metadata coverage, authenticated app-smoke source coverage for task CRUD and notes, profile/task/task-relationship/task-note/Stripe event/usage/rate-limit/daily-briefing grant source coverage, agent output history/review notes, agent-origin and agent-completed trace metadata, demo briefing refresh, Today focus/sidebar/briefing count alignment, mobile sidebar open/close behavior, local-date due-today behavior, All Tasks search/filtering/local-date sorting/cancelled-task review and restore, Upcoming grouping, Done clear/reload lifecycle, file-import preview/confirm flow, persistent appearance and browser reminder settings, auth guardrails, shared executable action-type helper coverage, reminder-task non-execution UI coverage, MCP/OpenAPI/Stripe guardrails, API-to-Connect-AI handoff coverage, DB-backed MCP handler and API-key validation coverage including audit preflight failure behavior, MCP/OpenAPI `cancelled` status contract alignment, demo CSV file import, login/signup demo-mode entry, authenticated app boot fallback consistency, task-capture/task-mutation/import/Done bulk-delete/agent-execution/billing server message surfacing, authenticated AI fallback notices, provider list-import failure handling, free-plan API-key UI gating, settings tab deep links, Connect AI paid-key/error-message gating and API-tab guidance, agent-execution quota status mapping, profile-update miss handling, MCP task not-found handling, focused task route validation contracts, and deterministic relative-date title cleanup
- `npm run test:e2e -- tests/e2e/mcp-tool-handlers.spec.ts` passed 11 tests including MCP `complete_task` task-row trace persistence
- `npm run smoke:routes -- --url=http://127.0.0.1:3002 --screenshot-dir=/tmp/nexdo-route-smoke` passed against the local dev server for marketing, app, login/signup/password-reset auth, import, settings, MCP setup, privacy, and terms routes at desktop and mobile widths
- `npm audit --audit-level=moderate`
- `npm run smoke:launch -- --skip-local --skip-providers --technical-only` passed as a partial technical launch smoke
- `git diff --check`
- PR #3 GitHub Actions Web rails passed on the latest inspected code head before this docs refresh, including the login-email normalization change; later pushes still require current PR-check inspection
- PR #3 Vercel preview deployment passed on an earlier inspected 2026-05-17 code head, but the latest inspected code head hit Vercel account build-rate limits and still needs fresh preview-deploy evidence

## Commands attempted but blocked
- `npm run verify:env` failed because `.env.local` is not present in this workspace. `.env.local.example` is present, but it is only the contract and cannot support provider smoke checks.
- `npm run smoke:routes -- --url=https://ph-nexdo-git-codex-launch-rea-42bdec-nathan-happywpcos-projects.vercel.app --screenshot-dir=/tmp/nexdo-preview-route-smoke` failed at desktop `/` because the latest preview is behind Vercel Deployment Protection. The route smoke supports `VERCEL_AUTOMATION_BYPASS_SECRET` and fails fast with that specific blocker.

## Current completion judgment
The Monday early-access demo, local build/test rails, demo task persistence, task-detail notes, local-date due-today behavior, file-import preview/confirm flow, agent output history/review notes, persistent appearance and browser reminder settings, task mutation guards, Done-page failure rollback, shared task route validation, AI input/output guards, authenticated agent execution guards, DB-backed MCP handler and API-key validation behavior, import quota guards, and billing route guards are in materially better shape and are locally verified. The broader objective is not complete as a production launch because provider-backed flows, real database/auth, Stripe billing, authenticated MCP tool execution, production deployment rails, and approvals remain unverified.

## Next required work
1. Configure a real Supabase/OpenAI/Stripe/app environment plus `VERCEL_AUTOMATION_BYPASS_SECRET` for protected Vercel previews, or use an unprotected preview URL, then run `npm run smoke:launch -- --env=.env.production.local --url=<preview-url> --technical-only`.
2. If debugging individual providers, run `npm run smoke:supabase -- --write`, `npm run smoke:app`, `npm run smoke:openai -- --app`, `npm run smoke:stripe -- --write --webhook`, and `npm run smoke:mcp -- --provision --write --audit` separately against the same target env.
3. After technical smokes pass, rerun the launch smoke against the production URL only after public-copy approval and production deploy verification can be represented with `--copy-approved --production-deploy-verified`.
4. Route public copy through Quill/founder approval before external launch use.
